<%= form_with url: "/manufactures/#{@manufacture.id}", method: :put do |form| %>
  <%= form.label :name, "Name" %>
  <%= form.text_field :name, value: @manufacture.name %>
  <%= form.submit %>
<% end %>