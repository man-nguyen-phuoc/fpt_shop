const button = document.getElementById('moca-111-tab');
const div = document.getElementById('moca-20');

button.onmouseover = function() {
  div.innerHTML = '<p>70% discount when paying via moca wallet on grap app.</p>'
};